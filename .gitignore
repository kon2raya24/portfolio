# Local-only resume variants tailored for specific applications.
# Kept on disk for attaching to emails, never committed to the public repo
# (would otherwise be indexed at the deployed portfolio URL).
resume-ats-*.html
resume-*-preview.png
Lemmuel-Turaya-CV*.pdf

# Video/social production material (scripts, captions, HeyGen scenes) —
# working files, not site assets; lives outside the deployed surface.
video/

# Editor/OS detritus
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/
